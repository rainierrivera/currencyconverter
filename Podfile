# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!
target 'rainierrivera' do
  pod 'Alamofire', '~> 4.8.0'
  pod 'SwiftyJSON', '~> 4.2.0'

  target 'rainierriveraTests' do
    inherit! :search_paths
    pod 'Nimble', '~> 8.0.0'
    pod 'Quick', '~> 2.0.0'
  end
end
